# 包查找模块
# 此文件用于统一管理项目依赖包的查找

# 定义一个函数，用于检查并查找所需的所有包
function(find_required_packages)
    # 在此添加项目所需的所有依赖包
    # 例如：
    # find_package(fmt REQUIRED)
    # find_package(spdlog REQUIRED)
    # find_package(nlohmann_json REQUIRED)
endfunction()
